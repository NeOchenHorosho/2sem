#include <iostream>
#include <string>
#include <vector>
using namespace std;

vector<int> find(char* source, char* word, size_t size_source, size_t size_word)
{

}
/*
чёто функция возвращает префикс совпадаюзий с суффиксом возвращает его

f(f(f(...f(x)))) 
смешная последовательность функций от функций
свойства последовательности:
1) ни один суффикс\префикс не пропустим
2) в какой-то момент станет пустой

Как он полещен в поиске?
Кажд
кнута-мориса


*/